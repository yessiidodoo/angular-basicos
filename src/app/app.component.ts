import { Component } from '@angular/core';

@Component({
  selector:'app-root',              // nombre del componete que va a tener
  templateUrl: 'app.component.html' // especificar el path del archivo html que será usado por este archivo.
  //template: '<span>Yessica</span>'  // permite mandar un string, separado por el html.
})
export class AppComponent {
  
}
